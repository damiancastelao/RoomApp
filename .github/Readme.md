# Personalización de MCP

Podemos acceder a este fichero `mcp.json` mediante el icono de herramientas al lado de "*Agent*"

```
{
"servers": {
    "github": {
        "url": "https://api.githubcopilot.com/mcp/",
        "requestInit": {
            "headers": {
            "Authorization": "Bearer github_pat_....fV16zII8U6ZV...."
            }
        }
    }
}
```